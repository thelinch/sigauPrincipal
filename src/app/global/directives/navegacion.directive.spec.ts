import { NavegacionDirective } from './navegacion.directive';

describe('NavegacionDirective', () => {
  it('should create an instance', () => {
    const directive = new NavegacionDirective();
    expect(directive).toBeTruthy();
  });
});
