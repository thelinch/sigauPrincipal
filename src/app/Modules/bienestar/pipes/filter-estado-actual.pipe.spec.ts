import { FilterEstadoActualPipe } from './filter-estado-actual.pipe';

describe('FilterEstadoActualPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEstadoActualPipe();
    expect(pipe).toBeTruthy();
  });
});
